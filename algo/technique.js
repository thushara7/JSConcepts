//**maintain a counter */
//1.find count
//2.find sum

//**reating a 'fast’ and 'slow’ pointer */
//------>In this technique, you initialise two pointers, one that increments by 1, and another that increments by 2.
//1.midpoint of a list of values.
//2. sorting algos to find middle

//**Loop through an array in reverse */

//**Use a Second Array */
//1. reverse a array

//**Use a “nested for-loop” */
//1.‘Bubble Sort’ algorithm:
//swap
var temp = array[i];
array[i] = array[i + 1];
array[i + 1] = temp;

//manipulation
array.push(value); // adds a value to end of array
array.pop(); // removes a value from end of array
array.unshift(); //adds a value to the beginning of an array
array.splice(startIndex, deleteCount, itemToAdd); // adds or removes value from specific place in array

//** Using Object */
